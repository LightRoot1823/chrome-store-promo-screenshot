# Chrome 商店宣传图 Skill（中文说明）

> 给人看的中文版。Agent 执行以同目录 [SKILL.md](SKILL.md) 为准；规则一致。

用真实产品界面截图，合成 Chrome 网上应用店 **1280 × 800 PNG** 宣传图。不要用 AI 编假界面顶替真实 UI。

安装与用法见 [README.zh.md](README.zh.md)。

## 什么时候用

- 做 Chrome / Edge 商店列表截图
- 已有原图，要加品牌名、主副标题、浏览器外框
- 要把「两步操作」合成一张（例如：工具栏按钮 → 结果弹窗）

## 开渲前先确认

缺什么问什么：

1. **原图路径**（一张或多张）
2. **品牌名**（如 `你的应用名`）
3. **主标题**（必填）
4. **副标题**（可选）
5. **构图模式**：`full` | `two-step` | `replace-overlay`
6. **主题**（默认 `teal-blue`）→ 见 [themes.zh.md](themes.zh.md)
7. **输出路径**（默认和原图同目录，如 `*-商店图-1280x800.png`）

主标题若为空，先问再渲。

## 固定版式

| 元素 | 规格 |
|------|------|
| 画布 | 1280 × 800 PNG |
| 背景 | 主题渐变 + 半透明圆形点缀 |
| 左上文案 | 品牌（约 22pt）→ 主标题（约 49pt 加粗）→ 副标题（约 20–21pt，可选） |
| 文字颜色 | 白色（主题必须够深） |
| 窗口 | 圆角白底 + 阴影；深色顶栏；红黄绿三键；假地址栏 |
| 内容区 | 真实截图，不重绘 UI 像素 |

同一套商店图用**同一个主题**。

## 构图模式

### `full`（默认）

- 原图按宽度放进内容区（约 1100px 宽）
- 只缩小、不放大
- 最多裁空白边

### `two-step`

- 底图：步骤 1 场景
- 浮层：步骤 2（优先「仅弹窗」）叠在右侧
- 可用 `①` / `②` 步骤标

### `replace-overlay`

- 已有合成图，只换浮层弹窗

## 清晰度原则

1. 原图尽量高清，往框里缩小
2. 不要默认把弹窗放大到半屏
3. **先 2×（2560×1600）渲染，再高质量缩回 1280×800**（见 `scripts/render_store_shot.swift`）
4. 网页小字糊：请用户重截或只截弹窗
5. Retina 上预览「实际大小」常放大 1280 图；可用约 50% 查看

## 实现（macOS）

1. 读 [themes.md](themes.md) / [themes.zh.md](themes.zh.md)
2. 用 [scripts/render_store_shot.swift](scripts/render_store_shot.swift)，保持 2× → 1×
3. 按任务改配置后 `swift` 运行
4. `sips` 确认 1280×800
5. 删除临时脚本，回报输出路径

非 macOS 用等价方案对齐版式；禁止假 UI 冒充产品截图。

## 命名

- 原图：用户自定
- 成品：`{功能}-商店图-1280x800.png` 或 `{feature}-store-1280x800.png`

## 不做的事

- 商店隐私问卷 / 权限理由
- 长描述文案（除非另有要求）
- 替用户在浏览器里截屏
